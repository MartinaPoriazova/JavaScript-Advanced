function processText(first, second, tirth) {
    let firstLength = first.length;
    let secondLength = second.length;
    let lirthLength = tirth.length;

    let sum = firstLength + secondLength + lirthLength;

    let avr = Math.floor(sum/3);

    console.log(sum);
    console.log(avr);
}